NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyCUD5zHL-FhLrIMOifiibetaJZnMe55JA0
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=crm-whitelab.firebaseapp.com
NEXT_PUBLIC_FIREBASE_DATABASE_URL=https://crm-whitelab-default-rtdb.firebaseio.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=crm-whitelab
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=crm-whitelab.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=184530357114
NEXT_PUBLIC_FIREBASE_APP_ID=1:184530357114:web:29c1050e223a18541c64c3

# Supabase (required for file uploads at build time)
NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder-key

# Email / Cron
CRON_SECRET=local-dev-secret-change-in-production
EMAIL_API_URL=http://localhost:3000
EMAIL_API_KEY=local-dev-key

# ---------------------------------------------------------------------------
# Ad platform integration (read-only Meta Ads / Google Ads campaign import)
#
# Server-only. None of these may be prefixed with NEXT_PUBLIC_: they must never
# reach the browser bundle. Leaving a platform's credentials empty simply hides
# its Connect button — the Campaigns page keeps working.
# ---------------------------------------------------------------------------

# 32-byte key, base64 or hex, used for AES-256-GCM encryption of stored tokens.
# Generate with: openssl rand -base64 32
ADS_TOKEN_ENCRYPTION_KEY=
# Separate secret used to HMAC-sign single-use OAuth `state` nonces.
# Generate with: openssl rand -base64 32
ADS_OAUTH_STATE_SECRET=

# Meta (Facebook) app with the `ads_read` permission only.
# Valid OAuth redirect URI: ${NEXT_PUBLIC_APP_URL}/api/ads/oauth/meta/callback
META_APP_ID=
META_APP_SECRET=
META_ADS_API_VERSION=v25.0

# Google Cloud OAuth client (Web application) + an approved Google Ads
# developer token. Read-only is enforced in code: no mutate call exists.
# Valid OAuth redirect URI: ${NEXT_PUBLIC_APP_URL}/api/ads/oauth/google/callback
GOOGLE_ADS_CLIENT_ID=
GOOGLE_ADS_CLIENT_SECRET=
GOOGLE_ADS_DEVELOPER_TOKEN=

# ---------------------------------------------------------------------------
# Background sync
#
# Schedule GET https://<site>/api/cron/ads-sync every 15 minutes with header
# `Authorization: Bearer ${CRON_SECRET}` (same scheduler as /api/cron/billing).
# The response returns `next_cursor` when more workspaces remain; call again
# with `?cursor=<next_cursor>` to continue. `?limit=` (1-100) sizes the batch.
#
# Realtime Database rules to add — these three roots are Admin-SDK only and
# must stay unreachable from any client:
#   "ad_connections":  { ".read": false, ".write": false },
#   "ad_campaigns":    { ".read": false, ".write": false },
#   "ad_oauth_nonces": { ".read": false, ".write": false },
#   "workspace_members": { ".indexOn": "user_id" }
# ---------------------------------------------------------------------------
